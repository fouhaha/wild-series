{#
ATTENTION : Ci-dessous, en commentaire, la première version de la vue wild/index.html.twig.
            Le contenu de cette vue est maintenant remplacé par ce qui suit plus bas.

<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Wild Séries</title>
    {{ encore_entry_link_tags('app') }}
</head>
<body>
    <header>
        {% block navbar %}
            {% include 'navbar.html.twig' %}
        {% endblock %}
    </header>
    <h1>{{ website }}</h1>
    {{ encore_entry_script_tags('app') }}
</body>
</html>

ATTENTION : Fin première version de la vue wild/index.html.twig.
#}

{% extends 'base.html.twig' %}

{% block title %}All programs{% endblock %}

{% block body %}
    <h1>Toutes les séries de la table program : </h1>
    {% for program in programs %}
        <div>
            <h2>{{ loop.index }} / {{ program.title }} - Catégorie : {{ program.category.name }}</h2>
            <p>{{ program.summary }}</p>
        </div>
    {% else %}
        Aucune série trouvée.
    {% endfor %}
    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{#   Ci-dessous, les tests des rojutes via les slugs, en redirection sur    #}
    <br>
    <a href="{{ path('wild_show', { 'slug': "walking-dead" }) }}">
        Testing show() method from WildController with a real program s slug.
    </a>
    <br>
    <a href="{{ path('wild_index') }}">
        Testing index() method from WildController to view all programs.
    </a>
{% endblock %}
