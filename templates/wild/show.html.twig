{#
ATTENTION : Ci-dessous, en commentaire, la première version de la vue wild/index.html.twig.
            Le contenu de cette vue est maintenant remplacé par ce qui suit plus bas.

<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ slug }}</title>
    {{ encore_entry_link_tags('app') }}
</head>
<body>
    <header>
        {% block navbar %}
            {% include 'navbar.html.twig' %}
        {% endblock %}
    </header>
    <h1>{% if slug == 'Aucune Sélection' %}
            Aucune série sélectionnée, veuillez choisir une série
        {% else  %}
            {{ slug }}
        {% endif %}
    </h1>
{{ encore_entry_script_tags('app') }}
</body>
</html>
#}

{# templates/wild/show.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}{{ slug }}{% endblock %}

{% block body %}

    <div class="media">
        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>
            <p>{{ program.summary }}</p>
            <p>Catégorie : {{ program.category.name }}</p>
        </div>
    </div>

    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>

{% endblock %}
